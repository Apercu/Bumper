'use strict';

describe('Service: Alert', function () {

  beforeEach(module('bumper'));

  var Alert;

  beforeEach(inject(function (_Alert_) {
    Alert = _Alert_;
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
